<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Order">
	<insert id="insertOrder" parameterType="userdto">
		INSERT INTO orderd (prod_name, prod_price, user_id, user_name,
		 user_addr,user_item, order_pay, order_status) VALUES (#{prod_name},
		#{prod_price}, #{user_id}, #{user_name}, #{user_addr},
		#{user_item}, #{order_pay},#{order_status})
	</insert>
	
</mapper>